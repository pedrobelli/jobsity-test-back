import { TaskBatchStatusUpdateDto } from './task-batch-status-update.dto';

describe('TaskBatchStatusUpdateDto', () => {
  it('should be defined', () => {
    expect(new TaskBatchStatusUpdateDto()).toBeDefined();
  });
});
